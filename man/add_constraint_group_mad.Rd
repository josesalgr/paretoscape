% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_constraint_group_mad.R
\name{add_constraint_group_mad}
\alias{add_constraint_group_mad}
\title{Add a group-level mean absolute deviation constraint}
\usage{
add_constraint_group_mad(
  x,
  max_mad,
  groups = NULL,
  actions = NULL,
  group_areas = NULL,
  tolerance = 0,
  name = "group_mad",
  dist_group_actions = NULL
)
}
\arguments{
\item{x}{A \code{Problem} object containing group-area distribution data.}

\item{max_mad}{Maximum admissible mean absolute deviation of group-level
selected-area proportions.}

\item{groups}{Optional subset of group identifiers.}

\item{actions}{Optional subset of actions.}

\item{group_areas}{Optional named numeric vector containing the denominator
area for every selected group. When \code{NULL}, denominators are derived by
summing \code{x$data$dist_groups$area} by group. Supply this argument whenever
\code{dist_groups} represents restorable rather than total group area.}

\item{tolerance}{Non-negative feasibility tolerance added to \code{max_mad}.}

\item{name}{Constraint-name prefix.}

\item{dist_group_actions}{Optional precomputed group-action area table with
columns \code{pu}, \code{group}, \code{action}, and \code{area}.}
}
\value{
A modified \code{Problem} object.
}
\description{
Add a group-level mean absolute deviation constraint
}
\details{
For each selected group \code{g}, the function defines the selected proportion

\code{p[g] = selected_area[g] / group_area[g]},

its unweighted mean \code{mu}, and an absolute-deviation variable \code{d[g]}. The
compiled MILP contains the linear constraints

\code{d[g] >= p[g] - mu}, \code{d[g] >= mu - p[g]}, and
\code{sum(d[g]) <= n_groups * (max_mad + tolerance)}.

The mean is endogenous. This function adds only a constraint; it does not
add a MAD objective.
}
