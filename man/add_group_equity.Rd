% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_group_equity.R
\name{add_group_equity}
\alias{add_group_equity}
\title{Configure group-equity data}
\usage{
add_group_equity(
  x,
  dist_group_actions,
  amount_col = "area",
  area_unit = c("m2", "ha", "km2"),
  fixed_amounts = NULL,
  actions = NULL,
  exclude_groups = NULL,
  name = "group_equity"
)
}
\arguments{
\item{x}{A \link{Problem} object.}

\item{dist_group_actions}{A data frame with columns \code{pu}, \code{group}, \code{action}, and
an amount column. Rows describe how much of a group is affected when an
action is selected in a planning unit.}

\item{amount_col}{Name of the amount column in \code{dist_group_actions}.}

\item{area_unit}{Unit of the action-specific amounts. Group denominators are
already stored internally in square metres by \code{\link[=add_groups]{add_groups()}}.}

\item{fixed_amounts}{Optional named numeric vector of group amounts that
contribute irrespective of decision variables, expressed in \code{area_unit}.
Do not use this when they are already represented by locked decisions.}

\item{actions}{Optional action ids or action sets to include. \code{NULL} uses all
actions represented in \code{group_actions}.}

\item{exclude_groups}{Optional group ids excluded from all equity metrics.}

\item{name}{Name of the equity population.}
}
\value{
A modified \link{Problem} object.
}
\description{
Configure how groups previously registered with \code{\link[=add_groups]{add_groups()}} enter equity
metrics. \code{add_groups()} remains the sole source of the group catalogue and
available-area denominators; this function adds only action-specific
contribution coefficients and equity-population choices.
Groups with no selected amount remain part of the equity population. Groups
that must not be evaluated (for example, structurally locked-in owners) can
be removed explicitly with \code{exclude_groups}.
}
