% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_constraint_group_area.R
\name{add_constraint_group_area}
\alias{add_constraint_group_area}
\title{Add group-level area constraints}
\usage{
add_constraint_group_area(
  x,
  target,
  sense = c("min", "max", "equal"),
  groups = NULL,
  actions = NULL,
  relative = TRUE,
  tolerance = 0,
  name = NULL,
  dist_group_actions = NULL
)
}
\arguments{
\item{x}{A \code{Problem} object containing group-area distribution data.}

\item{target}{Numeric scalar or named numeric vector.}

\item{sense}{One of \code{"min"}, \code{"max"}, or \code{"equal"}.}

\item{groups}{Optional subset of group identifiers.}

\item{actions}{Optional subset of actions.}

\item{relative}{Whether targets are proportions of the total area
available to each group.}

\item{tolerance}{Equality tolerance.}

\item{name}{Optional constraint-name prefix.}

\item{dist_group_actions}{Optional precomputed group-action area table.
If supplied, it must contain columns \code{pu}, \code{group}, \code{action}, and \code{area},
where \code{area} is the exact area of the intersection
\verb{PU ∩ action ∩ group}. This is useful when actions cover only part of a
planning unit and the exact group-action coefficients have already been
computed externally.}
}
\value{
A modified \code{Problem} object.
}
\description{
Add group-level area constraints
}
\details{
This function stores group-level area constraints. Group areas must have been
added previously with \code{\link[=add_groups]{add_groups()}}. Internally, group areas are represented
by \code{x$data$dist_groups$area}, which is interpreted as the area of group \code{g}
within planning unit \code{i}.

If \code{relative = TRUE}, each target is interpreted as a proportion of the total
area available to the corresponding group. If \code{relative = FALSE}, each target
is interpreted as an absolute area in the same internal units used by
\code{dist_groups$area}.

When \code{actions} is not \code{NULL}, the constraint applies only to the selected
subset of actions. If \code{dist_group_actions} is supplied, the optimization model
will use these precomputed exact group-action coefficients instead of deriving
them spatially downstream.
}
